package com.bagel.rosewood.core.registry;
/*package core.registry;

import common.world.gen.feature.RosewoodTreeFeature;
import core.Rosewood;
import net.minecraft.world.gen.feature.Feature;
import net.minecraft.world.gen.feature.NoFeatureConfig;
import net.minecraftforge.common.IPlantable;
import net.minecraftforge.event.RegistryEvent;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;

@Mod.EventBusSubscriber(modid = Rosewood.MODID, bus = Mod.EventBusSubscriber.Bus.MOD)
public class RosewoodFeatures {
	public static Feature<?> ROSEWOOD_TREE = new RosewoodTreeFeature(NoFeatureConfig::deserialize, false).setSapling((IPlantable) RosewoodBlocks.ROSEWOOD_SAPLING.get()).setRegistryName("rosewood_tree");
	
	@SubscribeEvent
    public static void registerFeatures(RegistryEvent.Register<Feature<?>> event) {
        //FlowerVillagePools.init();
        event.getRegistry().registerAll(
        		ROSEWOOD_TREE
        );
    }

}*/

